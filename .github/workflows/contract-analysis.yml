name: "Contract Security Analysis"

on:
  push:
    branches:
      - master
      - develop
  pull_request:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    name: "Deprecation notice"
    steps:
      - run: echo "Backstage integration removed. This workflow does nothing and can be removed."
