[package]
name = "benchmark_contract"
version = "1.0.0"
authors = ["Aurora <hello@aurora.dev>"]
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]

[target.'cfg(target_arch = "wasm32")'.dependencies]
lol_alloc = { version = "0.3.0", default-features = false }
